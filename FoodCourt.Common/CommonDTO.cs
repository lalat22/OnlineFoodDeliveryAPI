namespace FoodCourt.Common
{
    public enum UserRoleEnum
    {
        Admin = 1,    // Admin role will have RoleId = 1
        Customer = 2, // Customer role will have RoleId = 2 (default for new users)
        Vendor = 3    // Vendor role will have RoleId = 3
    }
}
